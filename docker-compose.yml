version: "3.9"

services:
  scraper:
    image: ${IMAGE_NAME:-careerpolitics/scraper}:${VERSION:-latest}
    container_name: careerpolitics-scraper
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8080}:8080"
    restart: unless-stopped
